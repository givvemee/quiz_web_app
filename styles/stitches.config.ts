import { createStitches } from '@stitches/react';

export const { styled, getCssText, keyframes } = createStitches({
  theme: {
    colors: {
      primary: '#8A98F1',
      subPrimary: '#279CF9',
      white: '#FFFFFF',
      black: '#000000',
      grayCCC: '#CCCCCC',
      warning: '#FF6961',
    },
  },
});
